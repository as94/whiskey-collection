import subscriptionContent from './subscription.html';
import './subscription.css';

const element = document.getElementById('subscription');
if (element) {
  element.innerHTML = subscriptionContent;
}
